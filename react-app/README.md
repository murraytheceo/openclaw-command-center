# Murray's Command Center

## 🚀 Deploy to Vercel (GitHub-Free)

### Option 1: Drag & Drop (Easiest)
1. Run: `npm install && npm run build`
2. Go to [vercel.com](https://vercel.com) 
3. Drag the `out/` folder to deploy
4. Done! ✅

### Option 2: Vercel CLI
1. Install: `npm install -g vercel`
2. Run: `npm install && npm run build`
3. Deploy: `vercel --prod`

## 🎨 Design
- **Theme**: Light Japandi (warm linen background)
- **Layout**: Sidebar + Center Content + Notes Panel
- **Features**: Auto-saving notes, agent status, business tracking

## 🔧 Development
```bash
npm install
npm run dev
```

Visit: http://localhost:3000